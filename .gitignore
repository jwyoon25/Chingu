# Secrets — never commit API keys
.env
.env.*
# …but DO track the placeholder template (it carries no real secret).
!.env.example
Secrets.xcconfig
Secrets.plist
Secrets.swift
*.secret

# Xcode / Swift
.DS_Store
xcuserdata/
*.xcuserstate
DerivedData/
build/
.build/
