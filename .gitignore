# Secrets — never commit API keys
.env
.env.*
# …but DO track the placeholder template (it carries no real secret).
!.env.example
Secrets.xcconfig
Secrets.plist
Secrets.swift
# …but our app's Secrets.swift holds NO keys (it only reads env vars), so track it.
!Sources/Chingu/Secrets.swift
*.secret

# Xcode / Swift
.DS_Store
xcuserdata/
*.xcuserstate
DerivedData/
build/
.build/
